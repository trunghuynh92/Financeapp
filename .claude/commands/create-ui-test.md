Create a Puppeteer UI test for: $ARGUMENTS

Follow these steps:
1. Create a test file in the tests/ directory
2. Use Puppeteer to simulate user interactions
3. Add assertions to verify expected behavior
4. Run the test and verify it passes
5. Commit the test file with a descriptive message

Use data-testid attributes for reliable element selection:
- Example: `await page.click('[data-testid="login-button"]')`
